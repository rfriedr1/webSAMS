# Exact, known-good dependency versions for SAMS Web.
#
# Generated from the working development environment
# (Python 3.13.13) on 2026-09-22. pyproject.toml keeps loose
# lower bounds for development; servers install from THIS file so a fresh
# install can never pull an untested release (Starlette 1.0 broke the app
# that way once).
#
#   python -m pip install -r requirements.lock.txt
#   python -m pip install --no-deps -e .
#
# Regenerate after deliberately upgrading and re-testing:
#   python -m pip freeze --exclude-editable  (then re-add the markers below)
# Platform markers: uvloop has no Windows build; colorama is Windows-only.
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
click==8.3.3
et_xmlfile==2.0.0
fastapi==0.136.1
h11==0.16.0
httptools==0.7.1
idna==3.15
Jinja2==3.1.6
MarkupSafe==3.0.3
openpyxl==3.1.5
pydantic==2.13.4
pydantic_core==2.46.4
PyMySQL==1.1.3
python-dotenv==1.2.2
python-multipart==0.0.28
PyYAML==6.0.3
SQLAlchemy==2.0.49
starlette==0.52.1
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.47.0
uvloop==0.22.1 ; sys_platform != "win32"
watchfiles==1.1.1
websockets==16.0
colorama==0.4.6 ; sys_platform == "win32"
